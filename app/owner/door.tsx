import { TabPlaceholder } from '@/components/TabPlaceholder';

export default function Page() {
  return <TabPlaceholder title="Door Management" body="Lock health, who unlocked when, and access overrides." />;
}
