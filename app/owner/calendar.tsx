import { TabPlaceholder } from '@/components/TabPlaceholder';

export default function Page() {
  return <TabPlaceholder title="Calendar" body="Hours, classes, holidays, and one-off events at a glance." />;
}
