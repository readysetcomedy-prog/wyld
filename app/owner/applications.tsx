import { TabPlaceholder } from '@/components/TabPlaceholder';

export default function Page() {
  return (
    <TabPlaceholder
      title="Applications"
      body="Post job openings to your public site's Careers page and review the applications that come in."
    />
  );
}
