import { TabPlaceholder } from '@/components/TabPlaceholder';

export default function Page() {
  return <TabPlaceholder title="Bookings" body="Upcoming reservations and a roster for each session." />;
}
