import { TabPlaceholder } from '@/components/TabPlaceholder';

export default function AdminCollaboration() {
  return (
    <TabPlaceholder
      title="Collaboration"
      body="Slack-style feeds for the WyLD team — channels, threads, image posts, @mentions, and unread alerts."
    />
  );
}
