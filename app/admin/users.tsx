import { TabPlaceholder } from '@/components/TabPlaceholder';

export default function Users() {
  return (
    <TabPlaceholder
      title="Users"
      body="All accounts across WyLD Inc — admins, gym owners, employees, and members. We'll build search and role management here."
    />
  );
}
